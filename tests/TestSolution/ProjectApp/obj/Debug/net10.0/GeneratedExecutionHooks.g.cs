// <auto-generated />
using ProjectCore;

namespace ProjectApp;

public static class GeneratedExecutionHooks
{
    public static void BeforeRun(WorkItem item)
    {
        _ = ScoreWorkItem(item);
    }

    public static int ScoreWorkItem(WorkItem item)
    {
        var score = 0;

        for (var attempt = 0; attempt < item.Priority + 3; attempt++)
        {
            if ((attempt & 1) == 0)
            {
                score += attempt;
            }
            else if (attempt % 3 == 0)
            {
                score += attempt * 2;
            }
            else
            {
                score += attempt * 3;
            }

            score += item.Name.Length > attempt ? item.Name.Length : attempt;
        }

        return score;
    }
}
